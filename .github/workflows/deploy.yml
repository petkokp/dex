name: Deploy

on:
  push:
    branches: [develop, master]

jobs:
  build-react:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install dependencies
      run: npm install
    - name: Lint
      run: npm run lint --if-present
    - name: Build
      run: npm run build --if-present
    - name: Deploy to Github Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
        publish_dir: ./build

  build-contracts:

    runs-on: ubuntu-latest
    env:
      PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
      INFURA_ENDPOINT: https://kovan.infura.io/v3/1d4972f6179e45efbff08d17e5d658ff

    defaults:
      run:
        working-directory: contracts

    strategy:
      matrix:
        node-version: [16.x]
        
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install dependencies
      run: npm install
    - name: Install Truffle
      run: npm install truffle -g
    - name: Install dependencies
      run: npm install
    - name: Run Truffle test
      run: truffle test --network kovan
    - name: Run Codechecks
      run: npx codechecks
      env:
        CC_SECRET: ${{ secrets.CC_SECRET }}
    - name: Run Test Coverage
      run: truffle run coverage --network kovan
    - name: Deploy contracts to Kovan
      run: truffle migrate —-network kovan
